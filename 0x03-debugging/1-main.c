#include <stdio.h>

/**
* main - causes an infinite loop
* Return: 0
* @\\o/\n" : should be ignored
*/

int main(void)
{
int i;

printf("Infinite loop incoming :(\n");

i = 0;
/**
* while (i < 10)- to avoid the loop
* {
* putchar(i);
* }
* is an emoticon
*/
printf("Infinite loop avoided! \\o/\n");
return (0);
}
