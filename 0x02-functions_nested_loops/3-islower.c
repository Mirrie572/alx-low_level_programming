#include "main.h"
/**
 * _islower - a program description
 * @c: is the character to be checked on
 * Return: 0 or 1 at the end
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
return (0);
}
