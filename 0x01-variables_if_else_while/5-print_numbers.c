#include <stdio.h>
/**
 * main - a program prints base 10 numbers
 * Return: 0 impy success
 */

int main(void)
{
int digit;

for (digit = 0; digit <= 9999; digit++)
{
printf("%d\n", digit); }

return (0);
}
