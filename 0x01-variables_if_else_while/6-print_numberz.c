#include <stdio.h>
/**
 * main - a program prints base 10 numbers
 * Return: 0 impy success
 */

int main(void)
{
int digit;

for (digit = 0; digit < 10; digit++)
{
putchar('0' + digit); }
{
putchar('\n');
}

return (0);
}
