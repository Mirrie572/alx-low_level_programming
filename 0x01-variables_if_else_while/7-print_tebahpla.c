#include <stdio.h>
/**
 * main - a program prints base 10 numbers
 * Return: 0 impy success
 */

int main(void)
{
char letter;

for (letter = 'z'; letter >= 'a'; letter--)
{
putchar(letter); }

putchar('\n');

return (0);
}
